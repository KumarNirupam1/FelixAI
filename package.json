{
  "name": "jarvis",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "dev": "turbo run dev --filter=@jarvis/desktop",
    "dev:web": "turbo run dev --filter=@jarvis/web",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "icon": "node scripts/make-icon.mjs",
    "postinstall": "node scripts/postinstall-electron.cjs"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.6.3"
  }
}
